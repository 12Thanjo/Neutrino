

this.importScene = function(scene){
	
};



this.exportScene = function(){
	local output = [];


	this.environment.forEach(@(id, conponents)->{
		local entity = this.environment.getPointer(id);
	});

	return output;
};